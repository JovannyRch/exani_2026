name: exani
description: "Quiz/Exam app built with Flutter — template base."

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.2
  syncfusion_flutter_pdfviewer: ^31.1.19
  dio: ^5.9.0
  path_provider: ^2.1.5
  open_filex: ^4.7.0
  google_mobile_ads: ^5.1.0
  audioplayers: ^6.1.0
  sqflite: ^2.4.2
  fl_chart: ^0.70.2
  intl: ^0.19.0
  flutter_local_notifications: ^19.5.0
  timezone: ^0.10.1
  shared_preferences: ^2.5.3
  share_plus: ^12.0.1
  in_app_review: ^2.0.11
  in_app_purchase: ^3.2.3
  supabase_flutter: ^2.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_native_splash: ^2.4.6
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/files/guia_manejo.pdf
    - assets/sounds/

flutter_native_splash:
  color: "#121212"
  image: assets/logo.png
  android: true
  ios: false
  web: false
